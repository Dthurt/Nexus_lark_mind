[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
pythonpath = .
testpaths = tests
markers =
    nlm_boot: nlm boot console smoke (dedicated CI step)
addopts = -m "not nlm_boot"
