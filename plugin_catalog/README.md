# Local plugin catalog

Drop installable packs here (each folder needs `manifest.json`).  
The Dock **市场** tab lists these plus bundled `plugins_volume` plugins.

See [plugin-packaging.md](../docs/plugin-packaging.md).
